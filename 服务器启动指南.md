# 服务器启动指南

本文档提供了如何启动和管理 GESP C++ 学习计划系统的后端服务器的详细步骤。

## 1. 基本启动步骤

### 1.1 启动后端服务器
1. 打开命令提示符或 PowerShell
2. 导航到 backend 目录：
   ```powershell
   cd "d:\paitou\Trae\GESPC++_studyPlan\backend"
   ```
3. 运行以下命令启动服务器：
   ```powershell
   python start_server.py
   ```
4. 服务器将在 `http://127.0.0.1:5000` 上运行

### 1.2 访问前端页面
1. 打开浏览器
2. 导航到前端登录页面：
   ```
   d:\paitou\Trae\GESPC++_studyPlan\frontend\login.html
   ```
3. 使用以下演示账号登录：
   - 用户名：`demo`
   - 密码：`123456`

## 2. 在后台运行服务器

### 2.1 使用 PowerShell 在后台启动
1. 打开 PowerShell 窗口
2. 运行以下命令：
   ```powershell
   Start-Process python -ArgumentList "start_server.py" -WorkingDirectory "d:\paitou\Trae\GESPC++_studyPlan\backend" -NoNewWindow
   ```

### 2.2 使用 cmd 命令在后台启动
1. 打开命令提示符
2. 运行以下命令：
   ```cmd
   cd /d "d:\paitou\Trae\GESPC++_studyPlan\backend"
   start /B python start_server.py
   ```

## 3. 查看服务器状态

### 3.1 查看 Python 进程
```powershell
Get-Process python
```

### 3.2 查看端口占用
```powershell
netstat -ano | findstr :5000
```

## 4. 停止服务器

### 4.1 通过进程 ID 停止
1. 首先获取进程 ID：
   ```powershell
   Get-Process python
   ```
2. 然后停止进程：
   ```powershell
   Stop-Process -Id <进程ID>
   ```

### 4.2 通过端口停止
1. 首先获取端口对应的进程 ID：
   ```powershell
   netstat -ano | findstr :5000
   ```
2. 然后停止进程：
   ```powershell
   taskkill /F /PID <进程ID>
   ```

## 5. 数据库配置

### 5.1 SQLite 配置（当前使用）
- 配置文件：`backend\.env`
- 配置项：
  ```
  DATABASE_URL=sqlite:///gesp_study_plan.db
  ```

### 5.2 MySQL 配置（备用）
- 配置文件：`backend\.env`
- 配置项：
  ```
  # DATABASE_URL=mysql+pymysql://root:password@localhost/gesp_study_plan
  ```

## 6. 常见问题和解决方案

### 6.1 服务器无法启动
- **原因**：可能是端口被占用或依赖项缺失
- **解决方案**：
  1. 检查端口 5000 是否被占用
  2. 确保已安装所有依赖项：
     ```powershell
     python -m pip install -r requirements.txt
     ```

### 6.2 数据库连接错误
- **原因**：可能是数据库配置错误或数据库未初始化
- **解决方案**：
  1. 检查 `.env` 文件中的数据库配置
  2. 运行数据库初始化脚本：
     ```powershell
     python init_db.py
     ```

### 6.3 前端无法连接到后端
- **原因**：可能是后端服务器未运行或 CORS 配置错误
- **解决方案**：
  1. 确保后端服务器正在运行
  2. 检查浏览器控制台是否有 CORS 错误

## 7. 项目结构

```
GESPC++_studyPlan/
├── backend/          # 后端代码
│   ├── routes/       # API 路由
│   ├── app.py        # 应用入口
│   ├── init_db.py    # 数据库初始化
│   └── start_server.py # 服务器启动脚本
├── frontend/         # 前端代码
│   ├── core/         # 核心功能页面
│   └── login.html    # 登录页面
└── 服务器启动指南.md  # 本文档
```

## 8. 技术栈

- **前端**：HTML, CSS, JavaScript
- **后端**：Python, Flask
- **数据库**：SQLite (默认), MySQL (备用)
- **认证**：JWT

---

**注意**：本文档保存于项目根目录，方便随时查阅和使用。